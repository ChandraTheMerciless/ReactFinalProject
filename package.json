{
  "name": "reactfinalproject",
  "version": "1.0.0",
  "description": "",
  "main": "dist/app.js",
  "scripts": {
    "build": "babel src/components -d lib/components && babel lib/components -d dist && browserify lib/components/*.js -o dist/app.js",
    "start": "http-server -p 5000",
    "react-run": "babel src/components -d lib/components && babel lib/components -d dist && browserify lib/components/*.js -o dist/app.js && http-server -p 5000"
  },
  "author": "Chandra and Keith",
  "dependencies": {
    "babel-cli": "^6.10.1",
    "bootstrap": "^3.3.6",
    "http-server": "^0.9.0",
    "jquery": "^3.0.0",
    "react": "^15.2.1",
    "react-dom": "^15.1.0",
    "react-redux": "^4.4.5",
    "react-router": "^2.6.0",
    "redux": "^3.5.2"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.5.0",
    "browserify": "^13.0.1",
    "jquery": "^3.1.0"
  }
}

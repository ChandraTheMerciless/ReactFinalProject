import React from 'react'

export default class RedFlags extends React.Component {
    render() {
        return(
            <div>
            	<p>Welcome to red flags.</p>
        	</div>
        );
    }
}
